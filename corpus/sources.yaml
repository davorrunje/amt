# Curated Turing Digital Archive references to transcribe.
# Add entries by browsing https://turingarchive.kings.cam.ac.uk and noting the
# catalogue reference. `url` is optional — only needed when the item's page slug
# is not the default lowercased-dashed form (e.g. it lives under a section path).
# `title`/`type` are your labels; they feed the transcription's front-matter.
#
# Focus: informal correspondence — Turing's own voice writing to people he knew —
# which is what grounds the persona register (see docs/superpowers/turing-voice-guide.md).
sources:
  # Turing -> Robin Gandy (his doctoral student), 1952-4: letters and postcards
  # from Wilmslow. Personal material, comments on Gandy's thesis, and on AMT's
  # own work. The page lives under a section-prefixed slug, hence the url override.
  - ref: AMT/D/4
    title: Letters and postcards from Turing to Robin Gandy (1952-4)
    type: correspondence
    url: https://turingarchive.kings.cam.ac.uk/correspondence-amtd/amt-d-4
